---
doc_type: ops-log
date: YYYY-MM-DD
---

# 过程日志 YYYY-MM-DD

> 路径：`logs/ops/YYYY-MM-DD.md`。不是事实源，记对话过程（用户摘要、动作、改了哪些文件、出处），方便回溯。
> 本轮有用户消息就写一行，禁止等整批结束再补。禁止全未知行。
> 单文件 **300 行**封顶；超限拆 `YYYY-MM-DD-p2.md`，索引加一行。跨月整包进 `logs/archive/YYYYMM-ops.md`。
> 用量：宿主给了才写「模型 token 秒」；不给写「—」。禁止编造。token 是用量，不是密钥。
> 表 A、表 B 各 **7 列**，禁止加列。
> 已存在的旧列日文件整文件冻结只读；当天还要记则用同日 `-p2` 新列。禁止同文件双表头。

## 表 A 对话流水

| 时间 | 用户摘要 | 本轮动作 | 改动文件 | 结果 | 出处 | 用量 |
|---|---|---|---|---|---|---|
| HH:MM / 会话序 | [≤80 字，禁止全文] | [查询/投喂/结转/指令/问答…] | [相对 ai/ 的路径，多个用分号；无则「无」] | 成功 / 失败 / 待确认 | 本项目 / 集层 / 集层→{项目名} | [模型 token 秒] / — |

## 表 B 字段错误

> 无错误则整表可省略，或另存 `YYYY-MM-DD-errors.md`（同样 300 行拆 `-p2`）。摘录 ≤80 字，禁止整段粘贴原文或条款。

| 时间 | 步骤 | 字段 | 摘录 | 处理 | 对象 | 结果 |
|---|---|---|---|---|---|---|
| HH:MM:SS | [对应表 A 动作] | [抽歪的字段名] | [≤80 字] | 待确认 / 忽略 / 已改 | [人名 / SRC-NNN / 稿文件名] | 成功 / 失败 / 入队 |
