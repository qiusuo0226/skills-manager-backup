# 项目信息完整性巡检规则

本文件定义 ChronoPM 的项目信息完整性巡检与补全提醒机制。在日常项目管理过程中，AI 持续检查事实源文件中的关键信息是否缺失、过期或冲突，并在信息缺口影响当前任务结论时主动提醒用户补充。

---

## 1. 定位与边界

### 1.1 与 14-self-check-rules.md 的区别

| 维度 | `14-self-check-rules.md` | 本规则（19） |
|------|--------------------------|-------------|
| 定位 | 文档处理后自查 | 项目信息完整性巡检 |
| 时机 | 处理日报、会议纪要、评审记录等文档**之后** | 日常查询、报告、风险分析、计划推导**时** |
| 对象 | 当前刚处理的文档及其提取结果 | 全部事实源文件中的关键字段 |
| 目标 | 确保本次处理没有遗漏、索引一致、关联正确 | 发现影响项目分析和决策的信息缺口 |
| 触发 | 文档处理完成后自动执行 | 条件触发，中提醒策略 |
| 输出 | 自查结果、遗漏项、修正建议 | 缺失项、影响说明、补充建议、是否现在补充 |

**两者互补，不合并。** 14 是"事后质检"，19 是"事前/过程中巡检"。

> **v3.4.0**：`reports/timeline/` 为懒建目录，**不得**因该目录不存在报 P0/P1。未生成过时间线报 = 正常空缺。

### 1.2 断言脚本旁路（v3.15.0）

完整性巡检开始时，若本机有 Python ≥3.9，先只读跑 P0 脚本（不写盘），用退出码替代「模型自述无遗漏」：

```bash
python scripts/verify_projection.py --root <项目根>
python scripts/verify_todo_continuity.py --root <项目根>
```

0=一致，1=差异，2=不可判。无 Python 则跳过脚本，仍做本文件其余人工巡检。

**施工禁区（v3.16.0）**：上列默认 P0 命令**不得**加 `--check-plan-section4`。该开关仅用于写入/生成 PLAN 落盘前。默认巡检中 §4 压缩最多 UNJUDGED（exit 2），不把存量计划打成 P0。

**不自动跑** `verify_requirement_wp.py` / `verify_contract_ri.py`（P1，用户明示再跑）。

本旁路 **不替代** 22 号全员结转。日报投喂仍走规则 Step 0（有 Python 则 `carryover_step0.py`，不是本旁路的 verify）。

### 1.3 日期格式与字段边界扫描（v3.16.0，P2）

巡检本轮触碰的 WP §7/§8 与计划 §4：

- 日期字段/区间两端非完整 `YYYY-MM-DD`（省年）→ 列入待纠正，标记 `⚠️ 日期格式待纠正`。不误伤 ID、`updated` 时分、周号、叙事口语。
- WP §8 执行人栏含日期特征（数字-数字或括号内区间）→ `⚠️ 字段边界待纠正`，不自动改写。`(点名)` / `(AI聚合)` / `⚠️待安排人` 合法。

默认 P2。当前任务正在投影/落盘该文件时升 P1。禁止全库无触碰回填。

---

## 2. 触发场景

### 2.1 强触发（必须执行完整性检查）

以下场景必须执行与当前任务相关的完整性检查：

- 生成日报、周报、时间线报（含自然月特例）
- 生成项目状态报告
- 分析延期风险
- 分析资源风险
- 分析成本或投入产出
- 推导排期
- 汇总里程碑状态
- 跨项目统筹分析（本包不巡检集层路径；请到 ChronoPM-Portfolio 对话）
- 导出 Excel/Word/PDF 报告
- 生成/管理待裁定事项（确认/驳回/巡检 `pm-decisions.md` 八块开放行；**仅块 8 子节「已经写了等点头」**与 Change Log `Confirmed By: 待确认` 一一对应，该子节禁 `auto` 行，不一致以 Change Log 重建该子节；块 1–7 以决策文件为权威。横幅 N=须人裁定开放行之和，`auto` 不计）
- 最近合法日 inbox 非空：本轮 **AUTO 跑 C'**（无并发），不向人派单；claim 直接删。报告只列本次自动合并了哪些残留稿
- 检查 Change Log 活跃区归档阈值（活跃区超 50 行或超 30 天触发按月归档到 `change-log/archive/YYYYMM-change-log.md` 并更新 `change-log/index.md`）

### 2.2 弱触发（只检查与当前问题直接相关的信息）

- 查询单个需求状态
- 查询单个任务进度
- 查询单个资源分配
- 查询单个里程碑状态
- 打开/查询正常计划或 WP 进度时：指针 14 号 D38（待确认却已入计划/已绑执行待办、可聚合未聚合、双计划歧义、废弃残留）。本文件不复制 D38 正文
- 查询计划进展

### 2.3 不触发

- 用户闲聊
- 用户只要求格式转换
- 用户明确要求"只回答问题，不做检查"
- 用户处于静默模式

---

## 3. 检查维度

### 3.1 合同层

检查文件：`context/project-context.md`, `context/project-brief.md`

| 字段 | 缺失影响 | 默认严重程度 |
|------|----------|-------------|
| 合同名称 | 无法形成项目归属和合同口径 | P1 |
| 合同总额 | 无法进行成本、收入、投入产出分析 | P0 |
| 合同类型 | 无法判断交付责任和验收风险 | P2 |
| 合同范围摘要 | 无法判断需求是否超范围 | P1 |
| 立项时间 | 无法判断项目周期起点 | P1 |
| 启动时间 | 无法判断项目真实执行周期 | P1 |
| 计划完工时间 | 无法判断延期风险 | P0 |

### 3.2 项目层

检查文件：`context/project-index.md`, `context/project-brief.md`

| 字段 | 缺失影响 | 默认严重程度 |
|------|----------|-------------|
| 项目名称 | 无法建立项目结构 | P0 |
| 项目经理 | 无法定位责任人 | P1 |
| 项目状态 | 无法生成准确项目总览 | P1 |
| 优先级 | 无法做项目排序和资源倾斜判断 | P2 |
| 启动日期 | 无法判断项目执行周期 | P1 |
| 计划完成日期 | 无法判断项目延期风险 | P0 |
| 实际完成日期 | 无法判断项目闭环状态（仅对已完成项目） | P2 |
| 依赖关系 | 无法识别跨项目阻塞 | P1 |
| 计划阶段数 | 无法关联 PLAN 文件 | P2 |

### 3.3 计划层

检查文件：`plans/PLAN-NNN-{name}.md`（若已创建；计划概览在 `context/project-brief.md`）

| 字段 | 缺失影响 | 默认严重程度 |
|------|----------|-------------|
| PLAN 编号 | 无法与需求、待办、资源建立稳定关联 | P0 |
| 计划名称 | 无法识别计划范围 | P1 |
| 计划开始时间 | 无法推导排期 | P1 |
| 计划结束时间 | 无法判断延期风险 | P0 |
| 需求数 | 无法判断工作量规模 | P1 |
| 需求 ID 列表 | 无法进行精细化需求追踪 | P2 |
| 资源 | 无法判断人力是否覆盖 | P1 |
| 状态 | 无法判断当前进展 | P1 |
| 里程碑型 WP | 无法判断计划与阶段目标的关系 | P2 |

### 3.3a 推导基线（v3.7.0）

检查：WP §3（功能点） 与 `project-context.md` 项目级推导规则 / 模块表。不检查已废 `entity-registry.md`（存在未迁 → 20 号门禁）。

| 检查项 | 缺失影响 | 默认严重程度 |
|------|----------|-------------|
| 多模块项目是否有 WP §3（功能点） 实体行 | 实体枚举依赖 AI 推断 | P2（多模块升 P1） |
| project-context 模块表是否维护 | 模块划分不清 | P2 |
| WP §3（功能点）「关联待办或需求」是否填充 | 推导后无法定位任务集 | P2 |
| WP §3（功能点） 当前状态是否与日报/纪要终态一致 | 推导过期 | P2 |

### 3.3b 源文档拆解台账（v3.7.0，存在性非强制）

已建 `sources/{编号}/` 时：台账缺行补行；分片目录须有 `_index.md`；parse-log 缺失作提示不阻断。

### 3.4 需求层

检查文件：`requirements/requirement-register.md`

| 字段 | 缺失影响 | 默认严重程度 |
|------|----------|-------------|
| 需求 ID | 无法追踪 | P0 |
| 需求描述 | 无法评估范围 | P0 |
| 优先级 | 无法排序 | P1 |
| 来源 | 无法判断需求合法性 | P2 |
| 所属计划/WP | 无法统计每个计划/WP 需求量 | P1 |
| 关联任务 | 无法判断需求是否进入执行 | P1 |
| 关联里程碑 | 无法判断需求交付节点 | P2 |
| 开发状态 | 无法判断实现进度 | P1 |
| 测试状态 | 无法判断质量状态 | P1 |
| 验收状态 | 无法判断交付闭环 | P2 |
| 总状态 | 无法生成项目健康度 | P1 |

> **双视图字段巡检（CR-20260815-001，存在性校验非强制必填）**：
> - `实现视图`、`原型/文档链接` 为可选字段（07 号 §8.10），缺失不记为强制项，仅对 **已确认状态 REQ** 统计覆盖率作巡检提示（P3，建议补充），不阻塞巡检结论。
> - `来源` 指向 Canonical ID 的需求，校验 Canonical 存在性；不存在的指针记 P2。
> - 不得将上述两项作为 P0/P1 强制缺失项，避免给存量工作区制造迁移负担。

### 3.5 任务层

检查文件：`todos/{date}/{owner}.md` 待办文件

| 字段 | 缺失影响 | 默认严重程度 |
|------|----------|-------------|
| 一级任务 | 无法形成任务结构 | P1 |
| 二级任务 | 无法跟踪执行粒度 | P0 |
| 完成百分比 | 无法生成进度统计 | P1 |
| 负责人 | 无法明确责任 | P1 |
| 计划开始/结束时间 | 无法判断延期 | P0 |
| 状态 | 无法判断执行情况 | P1 |
| 交付物 | 无法判断任务完成标准 | P2 |
| 偏差说明 | 无法解释计划偏差 | P2 |
| 整改措施 | 无法跟踪风险闭环 | P2 |

> **Requirement Ref 存在性巡检（CR-20260815-001）**：Requirement Ref 为 00 号 WF-8 定义的**可选字段，不得变相强制必填**；仅对已填 Requirement Ref 的待办校验其指向的 REQ 在 requirement-register 中存在且未取消，指向不存在的记 P2。需求关联覆盖率仅作统计提示（P3），不作为强制项。

### 3.6 资源层

检查文件：最新合法日（date≤今天）`todos/{date}/_index.md` **§1 花名册** + 有待办者的个人文件（§0 / §0.5）。禁止以已退役 `resource-register.md` 为覆盖检查源。

| 字段 | 缺失影响 | 默认严重程度 |
|------|----------|-------------|
| 姓名（§1） | 无法识别人员 | P0 |
| 岗位（§1） | 无法判断岗位覆盖 | P1 |
| 状态六态（§1） | 无法判断在组/空闲/出组 | P1 |
| 首次进组（§1） | 无法判断可用性 | P1 |
| 分配方式（§1） | 无法判断是否专职 | P2 |
| 缩写（§1 与 §6 一致） | TD 编号无法反查 | P1 |
| 有待办者 §0 联系方式/负责模块 | 身份细节不全 | P2 |
| 有待办者 §0.5 进出组 | 流转无法追溯 | P2 |
| B 角 / 同时参与（§1 备注） | 无法判断单点或跨项目风险 | P2 |

> **覆盖口径**：花名册 §1 = 全员名录。应建档（六态除已出组）缺当天个人文件 = 巡检问题。空 §1 待办表不算违规。已出组仍不建、不因缺 §0 报 P0。

### 3.7 里程碑层

检查文件：`project-info/progress-plan.md`

| 字段 | 缺失影响 | 默认严重程度 |
|------|----------|-------------|
| 里程碑 ID | 无法稳定引用 | P0 |
| 里程碑名称 | 无法识别阶段目标 | P1 |
| 计划日期 | 无法判断阶段排期 | P0 |
| 实际日期 | 无法判断是否按期完成 | P1 |
| 状态 | 无法判断阶段进展 | P1 |
| 交付物 | 无法判断完成内容 | P2 |
| 通过标准 | 无法判断是否真正达成 | P2 |

---

## 4. 严重程度分级

| 等级 | 名称 | 说明 | 提醒策略 |
|------|------|------|----------|
| P0 | 阻塞级 | 缺失后无法完成当前任务 | 必须提醒，说明当前任务无法准确完成 |
| P1 | 高影响 | 可以回答，但结论可靠性明显下降 | 主动提醒，可继续但需标注结论限制 |
| P2 | 中影响 | 不影响当前回答，但影响后续管理质量 | 回答后简短提示，或在巡检报告中列出 |
| P3 | 低影响 | 建议补充，但不主动打断 | 默认不主动提醒，仅在完整性巡检报告中展示 |

### 待裁定事项超期分级（主动变更 + 人工确认）

`pm-decisions.md` **八块开放行**按等待时长升级提醒，合入 P0-P3（不只块 8）：

| 等待时长 | 提醒等级 | 行为 |
|---|---|---|
| 超 7 天未裁定 | P1/P2 | 进入日常提醒（每日催办） |
| 超 14 天未裁定 | P0/P1 | 升级为 **critical**，较高优先级主动提醒，说明未裁定对延期判定/已完成统计的影响 |

### 动态升降级规则

- 如果当前任务涉及的字段缺失（即使是 P2 级别），升一级处理。
- 如果缺失字段在当前任务中完全不涉及，降一级处理。
- 多个 P2 缺失叠加在同一管理域，合并提醒并升为 P1。

---

## 5. 提醒格式

### 5.1 日常提醒模板

```text
我在处理这个问题时发现，当前项目资料中有一些关键信息不完整，会影响本次判断：

- 缺失项：[字段/文件/对象]
- 影响：[会影响什么判断、统计或推导]
- 建议补充：[建议用户补充的具体内容]

缺口已写入决策文件对应块，等你裁定；不是只在这句对话里说一声。
你可以现在补充，也可以回复"稍后补充"。
如果你希望本次只处理当前问题、不做完整性提醒，也可以说"本次不要提醒"。
```

### 5.2 多项缺失提醒模板

```markdown
在生成周报前，我发现当前资料有几项关键信息不完整：

| 缺失项 | 影响 | 严重程度 | 建议 |
|---|---|---|---|
| 5 个任务缺少完成百分比 | 会影响本周进度统计准确性 | P1 | 补充完成百分比 |
| M05 里程碑缺少计划日期 | 会影响阶段进度判断 | P0 | 补充计划日期 |
| 3 条高优先级需求未关联计划/WP | 会影响计划范围统计 | P1 | 补充所属计划/WP |

我可以先按现有信息处理，并在"数据不完整说明"中标注限制。
你要现在补充，还是先处理？
```

---

## 6. 用户控制

### 6.1 用户指令

| 用户说 | 效果 |
|--------|------|
| "先不提醒" | 当前这次不提醒，下次恢复 |
| "本次不要检查完整性" | 当前任务跳过完整性检查 |
| "进入静默模式" | 持续不提醒，直到用户退出 |
| "恢复完整性提醒" | 退出静默模式 |
| "给我做一次完整性巡检" | 立即执行全维度巡检并输出报告 |
| "列出当前项目缺失信息清单" | 等同于完整性巡检 |
| "只检查计划层" | 仅检查指定维度 |

### 6.2 静默模式

- 用户说"进入静默模式"后，AI 在当前会话中不再主动提醒。
- 但如果遇到 P0 级缺失且当前任务无法完成，仍必须提示（不可静默）。
- 用户说"恢复完整性提醒"后恢复正常。

---

## 7. 完整性巡检报告

当用户要求巡检或 AI 判断需要输出完整报告时，输出以下格式：

```markdown
# 项目信息完整性巡检报告

## 总体结论

当前项目资料完整度：高 / 中 / 低

主要影响：
- [影响项1]
- [影响项2]

## 缺失信息清单

| 层级 | 对象 | 缺失项 | 影响 | 严重程度 | 建议动作 |
|---|---|---|---|---|---|
| 合同层 | project-context.md | 计划完工时间 | 影响延期判断 | P0 | 立即补充 |
| 计划层 | PLAN 文件（若已创建） | 计划结束时间 | 影响计划排期判断 | P1 | 优先补充 |
| 需求层 | requirement-register.md | 所属计划/WP | 影响需求统计 | P1 | 批量补充 |

## 优先补充建议

建议优先补充：

1. [P0 级缺失项1]
2. [P0 级缺失项2]
3. [P1 级缺失项1]

原因：这些字段会直接影响进度、资源、风险和里程碑判断。
```

---

## 8. 巡检执行规则

### 8.1 检查范围确定

- 强触发：检查与当前任务相关的所有管理域。
- 弱触发：只检查与当前查询直接相关的对象和字段。

### 8.2 检查执行流程

```
用户发起操作
  → AI 判断操作涉及的管理域（合同/项目/计划/需求/待办/资源/里程碑）
  → 读取相关事实源文件
  → 按检查维度表逐字段检查
  → 判断缺失项严重程度（含动态升降级）
  → 缺口写入 pm-decisions.md 对应块（禁止只在对话里说一声；禁止全库无触碰回填；本轮读写到的对象才补登记）
  → P0 → 必须提醒，可能阻塞当前任务
  → P1 → 主动提醒，标注结论限制
  → P2 → 回答后简短提示
  → P3 → 不提醒
  → 用户选择补充 / 稍后 / 忽略
  → 如补充，更新对应事实源文件
```

### 8.2a 残留 inbox 与无模板类型（v3.9.0）

- 最近合法日 `todos/{date}/inbox/` 非空 → **AUTO 跑 C'**（巡检无并发），不向人派单。claim 直接删。报告只列本次自动合并了哪些残留稿。同一稿连续 3 轮仍失败：巡检事实行再列一次，仍不派单、仍不删稿。
- **禁止新建无模板 `doc_type`。** 一级标题必须是该 `doc_type` Skill 包模板上的节；多出来的节 = 失败。缺个人文件 §0.5 / §0.6 = 失败（新建或当日触碰时补，不扫历史全量回填）。存量无模板类型（如 `milestones/`）巡检 SUGGEST 迁 backup，不代迁。

### 8.3 静默状态记录

在 `project-brief.md` front matter 中记录：

```yaml
completeness_status: partial  # high / partial / low
last_completeness_check: "YYYY-MM-DD"
silence_mode: false  # 是否处于静默模式
```

---

## 9. 与其他规则的关联

| 关联规则 | 关系 |
|----------|------|
| `00-pm-main-rules.md` §2.7 | 默认意图检测新增"完整性巡检/补全提醒"意图 |
| `10-update-trigger-rules.md` | 日常更新/查询后增加关联字段完整性检查 |
| `14-self-check-rules.md` | 14 是文档处理后即时质检，19 是日常操作时持续巡检，互补不合并。待裁定缺口双方都写入 `pm-decisions.md` 对应块，禁止只在对话里说一声 |
| `18-init-wizard-rules.md` | 向导完成后，日常巡检接管信息完整性持续检查 |
| `05-query-rules.md` | 查询类任务触发弱检查 |
| `01-daily-report-rules.md` | 日报/周报生成触发强检查 |
