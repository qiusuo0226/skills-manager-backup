# 自查与完整性校验规则

本规则用于确保 AI 在处理项目文档时不遗漏关键事项。项目管理必须严谨，AI 必须对每次处理结果进行自查校验，发现遗漏时主动补充。

---

## 1. 核心原则

1. **索引预建，不是边查边建。** 索引在日常更新时自动维护，查询时直接读取，不在查询时临时构建。
2. **处理即校验。** 每次处理日报、会议纪要、评审材料、文件导入后，必须执行自查清单。
3. **遗漏即补充。** 自查发现遗漏时，立即补充，不等用户发现。
4. **不确定即标注。** 无法确认是否遗漏时，明确标注不确定项，不隐瞒。
5. **可追溯。** 每次自查结果记录在 AI 操作日志中。

---

## 2. 索引预建规则

### 2.1 禁止边查边建

AI 查询时**不得**：
- 临时创建脚本扫描目录
- 逐个读取日报文件搜索关键词
- 临时读取 Excel 文件提取信息
- 在查询过程中构建索引

AI 查询时**必须**：
- 先读取对应索引文件；有 `context/brain.md` 且 `.state.json` 的 `facts_fingerprint` 一致则先读 brain
- 索引存在且未过期 → 直接返回结果
- 索引不存在 → 提示用户重建（不自行全量扫描）；缺 brain 不致命，读事实原文并声明 as-of
- 索引过期（>24h）→ 提示用户「索引可能过期，上次更新 X 小时前，建议重建」
- 写事实后有 Python 则 P-VIEWS，禁止只改 brain 当纠偏

### 2.2 索引自动维护时机

索引在以下场景**自动更新**，不需要用户专门要求：

| 触发场景 | 自动更新内容 |
|---|---|
| 日报处理完成后 | 原文经 inbox→C' 合入当天 `{owner}.md`；**明日计划只留当天原文，不落成待办行、不建未来日目录**（N-37）。工人**不得**写 `_index.md`。收尾由协调者重建 §3；核对当日 inbox 空（稿先合，claim/runs 删） |
| 会议纪要处理后 | 行动项经 inbox→C' 落待办；缺负责人进 `pm-decisions.md` 块 6。工人不写 `_index.md` |
| 待办状态变更后 | 同步状态到待办文件；绑定文件 `_index.md` 由协调者收尾重建，工人不写 |
| 日报处理完成后（报告侧） | **不再**维护 `YYYYMM/index.md`（v3.4.0 停维）。项目日报/周报/时间线报按 01 号存根范式；判重走 D23 |
| 待裁定事项写入时 | **写入前查重**：键 = **分块 + 对象编号 + 事项键**。命中 → 合并说明/时间，不追加、计数不加。无重复才在 `pm-decisions.md` 对应块追加一行。横幅 N = 须人裁定开放行之和（`auto` 不计） |
| 待裁定事项确认后 | 从开放块移除，追加「决策记录」。属块 8「已经写了等点头」的同时改事实源 Change Log `Confirmed By` 为 PM 姓名 |
| 待裁定事项驳回后 | 追加决策记录留驳回；属块 8「已经写了等点头」的同时恢复事实源原值 |
| 实体级联完成后 | 确认对应实体规则文件 §级联传播规则 中所有 AUTO 动作已执行、CHECK 结果已输出、SUGGEST 动作已加入建议更新清单 |

### 2.3 索引一致性检查

AI 在每次更新索引后，必须执行一致性检查：

| 检查项 | 规则 |
|---|---|
| 绑定文件 vs 待办文件 | `todos/{date}/_index.md` 中的待办概览状态必须与各人 `todos/{date}/{owner}.md` 待办清单状态一致 |
| 花名册 vs 当日参与 | 同文件 **§3 每人必须出现在 §1**；§3 File Ref 必须指向已存在的个人待办文件；**§3 扫描排除整个 `inbox/`** |
| 非法未来日目录 | 列出 `todos/{date>今天}/` 为非法（不当最新 index，不参与花名册/结转扫描） |
| 待办 vs 来源 | 每条正式待办的来源（日报正式进展 / 纪要行动项 / 已规划 WP 拆待办 / 变更批准等）可追溯。**明日计划不是当天待办来源**（N-37） |
| 日报月度索引（停维） | 存量 `YYYYMM/index.md` 可留不删；**不得**再追加、不得再做记录数一致性校验 |
| RI 登记册 vs 检索路由 | RI 范围判定查询前检查本项目 `requirements/contract-register.md` 非空且格式完整（字段齐全）；登记册中的 active 合同须能在本项目 canonical 索引找到。空登记册 → 触发补录引导，不臆造结论（CR-20260813-002）。跨项目检索请用 ChronoPM-Portfolio |
| 本项目禁写集层 | 本项目 `ai/` 内不得把 `portfolio/` 当事实源落点；发现 `portfolio/` 存储路径 → 不读写，提示跨项目归集请用 ChronoPM-Portfolio |

一致性检查补充（D11，`pm-decisions.md`）：
- **写入前查重（强制）**：键 = **分块 + 对象编号 + 事项键**；命中则合并说明/时间，不追加、计数不加。
- **块 8 子节「已经写了等点头」** 必须与各事实源 Change Log `Confirmed By: 待确认` **一一对应**。`auto` **禁止**出现在该子节；出现则以 Change Log 为准删行。不一致 → **以 Change Log 为准重建该子节**，输出差异清单。子节「还没写等准许」与人员状态提醒不以 Change Log 重建。
- **块 1–7 以 `pm-decisions.md` 为权威，禁止用 Change Log 重建。**
- 横幅 N = 八块开放行之和。禁止再用 `total_pending` 单指 Change Log 待确认条数。

> 注：`Confirmed By: 待确认` 为临时质保，不计入延期判定与已完成统计（见 `05-query-rules.md` §6.5/§6.6）。`auto` 按已确认计入。

### 2.3a `pm-decisions.md` 八块机制（v3.9.0，本文件为唯一完整定义）

路径：本项目 `ai/pm-decisions.md`（懒建）。一个 md，按缺口分块。工人禁写；只协调者写。多源按块追加：读整文件 → 按键查重合并 → 只追加或改命中行 → 写回。禁止写成只剩本源那一块。两协调者冲突：再读再合，禁止互盖。

**开放项八块（各块一张表，列 ≤7）**

1. 需求未确认（含三项缺口：做不做 / 效果 / 方案）
2. 需求未绑定工作包
3. 工作包待确认（绑定草案）
4. 已规划工作包无待办
5. 待办未绑定工作包
6. 待办缺负责人（不落无主待办）
7. 关联待办缺处理方式
8. 其他待裁定：含「已经写了等点头 / 还没写等准许」+ 人员状态提醒（22 号空闲）

**硬规则**

| 动作 | 规则 |
|---|---|
| 写入前查重 | 键 = 分块 + 对象编号 + 事项键。命中 → 合并说明/时间，不追加 |
| 与 Change Log 一一对应 | **只对块 8「已经写了等点头」**。不一致 → 以 Change Log 重建**该块**。块 1–7 以本文件为权威，**禁止**用 Change Log 重建 |
| 横幅 N | 八块开放行之和。白话「有 N 件事等你裁定」 |
| 确认 | 从开放块移除，追加决策记录；属块 8 已经写了等点头的同时改 Change Log `Confirmed By` |
| 驳回 | 决策记录留驳回；属块 8 已经写了等点头的同时恢复事实源原值 |
| 7/14 天催办 | 八块开放行都算，合入 19 号 P0–P3 |
| D21 / D22 | 写入对应块（块 5 / 块 2 或块 3），禁止只在对话里说一声 |

决策记录（已裁定，只追加）：`时间 | 类型 | 对象编号 | 问句摘要 | 裁定 | 影响`。不是聊天原文。活跃区 50 行/30 天 → `ai/pm-decisions-archive/YYYYMM.md`。会议上的拍板仍在 `decisions/decision-log.md`，不与本文件混表。

发现不一致时：
1. 以事实源为准。
2. 输出差异清单。
3. 建议修正索引。

### 2.4 索引派生分级

索引按其能否从事实源重建，分三类管理：

**完全派生索引**（可从事实源全量重建，实体变更时经级联规则 AUTO 触发重建）：
| 索引 | 来源 |
|---|---|
| `todos/{date}/_index.md` §3/§4（绑定派生） | 当日各人待办文件聚合（**排除整个 `inbox/`**）。**§1 花名册不是完全派生**（含无待办者），不得用当日文件重建覆盖。工人禁写 `_index.md` |
| `YYYYMM/index.md`（v3.4.0 停维） | 存量可留；新报告不再维护此派生索引 |

**增量维护索引**（变更时增量更新，非全量重建）：
| 索引 | 维护方式 |
|---|---|
| `change-log/index.md` | 归档时增量追加月份导航条目 |

**独立累积索引**（不可从单一事实源重建，维持现有"过期检测→提示"机制）：
| 索引 | 说明 |
|---|---|
| `outputs/index.md` | 追加式 |
| `pm-decisions.md` | 八块开放项 + 决策记录（独立累积；仅块 8 与 Change Log 待确认对齐） |

规则：
- 完全派生索引在事实源变更时通过级联规则 AUTO 触发重建。
- 增量维护索引不做全量重建，走增量 + 过期检测（§2.3 一致性检查衔接）。
- 独立累积索引不适用自动重建，维持"过期检测 → 提示"机制（§7）。
- 索引级联完整性由各实体规则文件 §级联传播规则 + 本文件 §2.2 末尾"实体级联完成后"行共同保障。

### 2.5 输出前级联验证（强制，仅限写入场景）

当工作流涉及事实源或派生视图的写入/更新动作时，AI 必须在输出前执行级联完成验证，流程见 `00-pm-main-rules.md` §8a.2（唯一权威定义，本文件不重复展开）。

纯查询场景（`10-update-trigger-rules.md` Level 4）不产生写入，不触发此验证。

---

## 3. 文档处理自查清单

### 3.1 日报处理自查清单

日报处理完成后，AI 必须逐项检查。工人身份**不得**写 `_index.md`（含 `logs/ops/_index.md`）；自查须核对当日 inbox 已空（非空则 AUTO 跑 C'，不得静默删稿、不得向人派单）。

| 序号 | 检查项 | 漏了会怎样 | 检查方法 |
|---|---|---|---|
| D1 | 今日完成事项是否全部提取为任务进展 | 任务进度不更新，待办文件滞后 | 逐条核对"今日完成"区块 |
| D2 | 进行中事项是否更新到待办文件进度 | 待办进度不反映实际情况 | 逐条核对"进行中"区块 |
| D3 | 明日计划是否只留在当天原文（不落待办行、不建未来日） | 违反 N-37：把明日计划落成当天待办或未来目录 | 核对「明日计划」只在当天原文；次日才由结转/T-A3 拉齐正式任务 |
| D4 | 阻塞事项是否全部识别为问题候选 | 问题遗漏，风险扩大 | 逐条核对"阻塞/问题"区块 |
| D5 | 潜在风险表述是否先出识别判定卡 | 见词直登或漏识别 | 检出信号词后须有判定卡；禁止直接写登记册 |
| D6 | 资源变动关键词是否触发资源联动 | 人员变动遗漏 | 扫描"请假""抽调""借调"等关键词 |
| D7 | 投入工时是否更新到待办文件工时列 | 工时统计不准 | 核对"投入工时"区块 |
| D8 | 日报月度索引已停维（v3.4.0） | 继续维护已废索引 | **不要**再追加 `YYYYMM/index.md` / `reports/daily/index.md`。缺 `reports/timeline/` 不报 P0。判重走 D23 |
| D9 | 绑定文件 §3 是否由协调者按当日个人文件重建（工人未写 index） | 工人抢写 index 或 §3 与目录不一致 | 工人禁写 `_index.md`；收尾核目录人名 vs §3（排除整个 `inbox/`） |
| D10 | 同人同天多次汇报是否检查了合并追加 | 工作日志内容丢失 | 确认同人同天汇报合并追加到同一待办文件工作日志段，未覆盖 |
| D11 | 待裁定：写入前按「分块+对象编号+事项键」查重（命中合并不追加）；**仅块 8 子节「已经写了等点头」**与 Change Log `Confirmed By: 待确认` 一一对应（禁 `auto` 行）；**块 1–7 不以 Change Log 重建** | 重复粘贴，或该子节与事实源不一致，或误用 Change Log 覆盖块 1–7 | 写入前查重；核对本次新增/确认/驳回；该子节不一致以 Change Log 重建**该子节** |
| D12 | Change Log 活跃区是否超 50 行或超 30 天未归档 | 活跃区膨胀、追溯困难 | 活跃区超 50 行或超 30 天触发按月归档到 `change-log/archive/YYYYMM-change-log.md` 并更新 `change-log/index.md` |
| D13 | 级联传播是否完整执行 | 下游同步遗漏 | 确认对应实体规则文件 §级联传播规则 中所有 AUTO 动作已执行、CHECK 结果已输出、SUGGEST 动作已加入建议更新清单 |
| D14 | WP §3（功能点） 与待办终态一致性 | 实体状态与事实源漂移 | 本轮触碰的 WP §3（功能点） 行 vs 待办终态事件；不一致则 SUGGEST 同步。不查已废 entity-registry |
| D15 | 每条正式待办（有 Owner + Deadline + 交付物）是否 WP Ref 恰好 1 个且非孤儿 | 待办孤儿、多 WP、数据不一致 | **限局部**（本轮触碰）。核对 WP Ref 恰好一个存在的已规划 WP；空/待绑定/none/多值 → 不落完成态，问绑哪个或拆分。禁止全库扫；未触碰历史行只读降级 |
| D16 | 是否存在“只在日报/纪要中呈现而未落待办文件”的半写状态 | 半更新残留 | 检查最近日报/纪要的正式行动项是否按 WF-8 落待办文件（一次性提醒除外）；发现半写 → 补全待办文件并输出差异清单 |
| D17 | 本项目内是否出现集层存储路径 `portfolio/` | 把 `portfolio/` 当本包事实源 | 扫描本项目 `ai/`；发现 `portfolio/` 存储路径 → 不读写，提示跨项目归集请用 ChronoPM-Portfolio |
| D18 | DF 行完整性 | 内置习惯基线丢失、不可审计 | 检查 `context/pm-profile.md` §1：DF-NNN 相对 21 号 §5.1a 基线表编号连续、无缺失、无任何 DF 行被移入 §3 已否决/已废弃段；缺失 → 从模板恢复该行（状态 `disabled` + 备注「检测到缺失已自动恢复」）并提示 PM |
| D19 | 日期格式 / §8 字段边界（v3.16.0） | 省年或执行人栏混入日期被静默投影 | 本轮写入的 WP/计划按 19 号 §1.3 扫描；细则以 19 号为准 |
| D19 | 操作类型×已加载规则匹配 | 规则未加载导致漏流程 | 对照 SKILL.md §6 最小规则集：当前操作类型必须已加载对应规则文件；缺失则先加载再继续，不得跳过流程 |
| D20 | WP 数据一致性巡检（v3.5.0） | WP 文件/索引/计划简表/待办引用漂移 | 扫描 `wps/WP-*.md` 与 `wps/_index.md` 双向核对：文件有而索引缺行 → **补行不删除**；索引有行而文件缺失 → 登记 `pm-decisions.md`。核对 plan_ref/状态/关联需求镜像。扫描本轮待办 WP Ref 指向的文件是否存在。不一致 → 写入对应块（禁止只在对话里说一声） |
| D21 | WP 绑定完整性（v3.5.0） | 待办未绑定 WP 或多值 | **限局部**：本轮读写过的待办文件 + 当日 `_index.md` 圈定的人。WP Ref 为空/`待绑定`/none/多值 → 写入 `pm-decisions.md` **块 5（待办未绑定工作包）** + DF-019 问绑哪个。新写禁止带空值落盘。禁止全库扫；禁止只在对话里说一声 |
| D32 | 待办时间盒越界（v3.10.0） | 待办结束晚于所属 WP 结束 | **限局部**（与 D21 同族）：本轮读写过的待办 + 当日 index 圈定的人。已绑 WP 且待办结束 > WP 结束 → 报漏检。`pm-decisions` 块 8 已有该 TD 的开放/已挂起裁定 → **不重复报**。开始早于 WP 开始不报（轻提示，非本项）。能耗补录回写已有**历史日**文件 §0.6 不报「历史不可变」违规（须已登记 pm-decisions 块 8 子节 1；非当日投喂）。禁止 `*-energy-*.md` |
| D33 | WP 状态链（v3.11.0） | 头与链不一致 | 无 §7 → P3（待补全）；链不变式破坏或头≠链尾 → P2。查询仍以链尾为当前，不改旧行。与 D20 同族（WP 一致性），方向为包内 |
| D34 | 工作区根散落生成物（v3.11.0） | 生成物落到与 `ai/` 平级 | 扫 `ai/` 父目录本轮新文件（非 `ai/`）→ P2，提出迁入 `ai/outputs/`，不留 |
| D35 | PLAN 结构（v3.14.0） | 自创章节/含 TD | 新建计划缺 **6 节**、非 `PLAN-*.md` 命名、含 TD、表>7 列、按状态分章、§3 含子行 → P2。存量未规范化 → P3 不强制当场改写（upgrade-to 清单；只处理 PLAN-*.md） |
| D36 | 计划投影对账（v3.11.0） | 正常计划 §3/§4 与 WP 漂移 | **本轮触碰**的 WP 及其正常计划全量比对当前状态/执行人/排期/关键阶段/§4 列表。禁止随机抽 3 个。不一致 P2 当场修 |
| D37 | plan_ref 互指（v3.11.0） | 正常计划 §3 与 WP plan_ref 不一致 | 多值用 ` / `。只计 status=正常。P2 当场修 |
| D38 | WP 联动健康（v3.13.0） | 待确认却已开工/人期未聚/双计划歧义/废弃残留 | **限本轮触碰 WP + 当日 index 圈人**。命中即告警并走 ADVANCE/SCAN/RETIRE：头或链尾=待确认但 plan_ref 含正常计划；头=待确认/已规划但已绑执行类待办≥1；正常计划 §3 当前状态≠链尾；剩余阶段有可匹配待办但 §8 人/期仍空；两份正常计划对同一正常 WP 人期状态不同（范围=该 WP **全部**正常计划，闸 1 并集）；正常计划 §3 仍列 `effect=废弃` 的 WP。§3 引用文件不存在 → **先 D20**，不直接当废弃移出。禁止全库扫所有 WP |
| D39 | WP §8 阶段非空（v3.14.0） | 阶段表空 | 本轮触碰 WP：§8 至少 1 行阶段。P2 |
| D40 | 阶段空岗显式（v3.14.0） | 🔄 无人或 ⏳ 空白 | 🔄 必须有执行人；⏳ 必须显式 `⚠️待安排人` 或「—」，禁止静默空白。P2 |
| D41 | 计划无子行（v3.14.0） | §3 子行残留 | 正常计划 §3 行数 = 纳入 WP 数（无 `└ ` 子行）。P2 当场删子行并补 §4 |
| D42 | 计划 §4 覆盖（v3.14.0） | §4 缺 WP | 计划 §4 列表覆盖所有 §3 纳入的 WP。P2 当场从 WP §8 补 |
| D22 | 需求蔓延巡检（v3.6.0） | 无需求 WP 或 REQ 无归属漏提醒 | **限局部**（本轮读写的 WP/REQ/登记册 + 当日待办索引圈定）。WP 无关联 REQ → `pm-decisions.md` 对应块（工作包无需求 / 块 3 或块 8，疑似蔓延）；REQ 无所属 WP → **块 2**。与 WF-8 溯源同一指针则合并，不追加。禁止全库扫；禁止只在对话里说一声 |
| D23 | 存根区间判重（v3.4.0） | 用旧存根外推或拼接编造 | **仅当次**生成涉及的 `reports/timeline/`（及当日读写的 daily/weekly 存根）：精确重合才直读；否则整段从 todos 重汇聚。缺 timeline/ 目录不报 P0 |
| D24 | sources/ 一致性（v3.7.0，限局部） | 指纹-台账-分片漂移 | 只扫本轮触碰的 `sources/{编号}/`：ledger 指纹 vs meta、分片索引 vs 文件、parse-log 存在性（非强制） |
| D25 | 非法未来日待办目录（v3.8.0） | 残缺未来 index 污染「最新花名册」 | 列出 `todos/{date>今天}/`；标非法，不当最新 index，本项不删目录 |
| D26 | 风险绑定与旧格式（v3.7.0） | 高风险无待办或旧格式滞留 | ① 高风险与开放问题须有绑定待办（或 PM 显式接受）；待办全完成但风险仍开放超 7 天 → 告警。② 检出 15 列表头/缺时间线/旧枚举 → 输出迁移清单，不允许旧格式无限期共存 |
| D27 | `_index` §3 ⊂ §1（v3.8.0） | 当日参与有人却不在花名册 | 当日 `_index`：§3 每人必须出现在同文件 §1 |
| D28 | File Ref 必须存在（v3.8.0） | 参与表指向幽灵文件 | §3 File Ref 路径必须存在对应 `todos/{date}/{owner}.md` |
| D29 | N-29 进度↔状态闭环 | 「进行中+100%」脏数据 | 进度=100% ⇒ 状态必须已完成；已完成 ⇒ 100%；标了 100% 即已完成，正文「收尾」不回退。存量非已完成+100% → 改已完成（先写后告知，登记 `pm-decisions.md` 块 8）。待办状态枚举无「待评审」 |
| D30 | N-30 日期完整性 | 开始晚于结束 | ① 进度 100% 非已完成 → 走 D29；② 日期倒挂：命中结转改写特征则恢复首次出现日（先写后告知，不问人）；判不了才问 PM，禁止自动对调；③ 两病并存先修日期再走 N-29 |
| D31 | 收尾 inbox/claim/runs 归零；工人未写 `_index` | 残留稿当第二份日报或丢失；工人抢写 index | 当日 inbox 无 `{owner}-*.md`（非空先 C'）；无 `.claim-*`；本 run 的 `logs/ops/runs/{run_id}/` 已不存在。工人禁写 `_index.md` 与 `logs/ops/_index.md` |

### 3.2 会议纪要处理自查清单

| 序号 | 检查项 | 漏了会怎样 | 检查方法 |
|---|---|---|---|
| M1 | 所有行动项是否提取为 TODO | 待办遗漏 | 逐条核对会议中的"行动项/安排/负责/跟进" |
| M2 | 所有决策是否记录到 decision-log | 决策遗漏 | 逐条核对"决定/确认/同意/不做/暂缓" |
| M3 | 提到的风险是否先出识别判定卡 | 见词直登或漏识别 | 先出判定卡，禁止见词直登 |
| M4 | 提到的问题是否识别为问题候选 | 问题遗漏 | 扫描问题关键词 |
| M5 | 提到的需求变更是否触发变更流程 | 变更遗漏 | 扫描"变更/调整/新增/取消" |
| M6 | 资源变动是否触发资源联动 | 人员变动遗漏 | 扫描资源关键词 |
| M7 | 待办索引是否同步更新 | 索引不完整 | 确认 todos/ 已更新 |
| M8 | 级联传播是否完整执行 | 下游同步遗漏 | 确认 Decision 级联传播规则中所有 AUTO/CHECK/SUGGEST 动作已处理或已加入建议清单 |

### 3.3 评审材料处理自查清单

| 序号 | 检查项 | 漏了会怎样 | 检查方法 |
|---|---|---|---|
| R1 | 评审结论是否提取为决策 | 决策遗漏 | 核对"通过/不通过/有条件通过/待确认" |
| R2 | 评审中的待确认事项是否提取为问题 | 问题遗漏 | 核对"待确认/待讨论/待补充" |
| R3 | 评审中发现的风险是否先出判定卡 | 见词直登或漏识别 | 先出判定卡 |
| R4 | 评审中的行动项是否提取为 TODO | 待办遗漏 | 核对"需要/应该/后续/跟进" |
| R5 | 需求变更是否触发变更流程 | 变更遗漏 | 核对需求状态变化 |
| R6 | 评审纪要是否归档 | 过程记录丢失 | 确认 meetings/ 或 reviews/ 已保存 |
| R7 | 级联传播是否完整执行 | 下游同步遗漏 | 确认评审触发的实体级联传播动作已处理或已加入建议清单 |

### 3.4 风险/问题追溯自查清单

当用户要求"追溯项目风险/问题"时，AI 必须执行多源交叉校验：

| 序号 | 检查项 | 方法 |
|---|---|---|
| T1 | 风险登记册是否覆盖所有来源 | 对比 risks/risk-register.md 与以下来源的差异 |
| T2 | 来源A：日报中的风险关键词 | 扫描所有日报索引，搜索"风险/可能/担心/如果" |
| T3 | 来源B：会议纪要中的风险讨论 | 扫描会议索引，搜索"风险/可能/担心/如果" |
| T4 | 来源C：周报中的风险章节 | 扫描最近周报的风险部分 |
| T5 | 来源D：问题登记册中的关联风险 | 检查 issues/ 中有关联风险编号的记录 |
| T6 | 来源E：待办文件中的延期/阻塞 | 检查待办文件中状态为已阻塞/已超期的任务 |
| T7 | 差异报告 | 输出"登记册有但来源无"和"来源有但登记册无"两个方向差异 |

**追溯输出格式：**

```markdown
## 风险追溯校验报告

### 校验范围
- 风险登记册：X 条
- 日报扫描范围：YYYY-MM-DD ~ YYYY-MM-DD，共 X 份
- 会议纪要扫描范围：最近 X 次会议
- 周报扫描范围：最近 X 周

### 交叉校验结果

| 风险编号 | 登记册 | 日报 | 会议 | 周报 | 问题关联 | 一致性 |
|---|---|---|---|---|---|---|
| R-001 | ✅ | ✅ | ✅ | ✅ | ✅ | 一致 |
| R-002 | ✅ | ❌ | ✅ | ✅ | - | 日报未提及（正常） |
| - | ❌ | ✅ | ❌ | ❌ | - | ⚠️ 日报提及但未登记，建议补充 |

### 遗漏项

| 来源 | 关键词 | 上下文 | 建议动作 |
|---|---|---|---|
| 日报 2026-08-05 仇索 | "担心接口联调延期" | "...接口还没通，担心下周联调会延期" | 建议新增风险候选 |

### 结论
- 登记册已有风险：X 条
- 发现遗漏候选：Y 条
- 建议确认后补充
```

---

## 4. 处理后自查输出格式

**输出分级（审计不弱化）**：
- **全部通过**：只输出一行摘要；全量自查表落盘文件变更日志 / AI 操作日志，不在对话展开。
- **任一项失败/遗漏/不确定**：对话输出完整自查表 + 发现项（不得省略）。

### 成功（一行摘要）

```markdown
自查通过：日报处理 D1–D31 全部通过。
```

### 失败（全量输出）

```markdown
## 自查清单

| 序号 | 检查项 | 结果 | 说明 |
|---|---|---|---|
| D1 | 今日完成事项提取 | ✅ 3/3 | 全部提取 |
| D5 | 风险信号是否先出判定卡 | ⚠️ 1 项遗漏 | "担心接口延期"见词未出判定卡，已补判定卡 |
| D11 | 待裁定查重/块 8 对齐 | ✅ | 无重复，开放行合计与横幅 N 一致；未用 Change Log 重建块 1–7 |
| D18 | DF 行完整性 | ✅ | DF 编号连续、无缺失、未移入废弃段 |
| D19 | 操作类型×已加载规则 | ✅ | 日报=00+01+06+17 已加载 |

### 自查发现
- [发现1]：风险信号"担心接口延期"初次未出判定卡，已补判定卡（未直登登记册）
```

---

## 5. 多轮自查规则

### 5.1 首次处理后的自查

首次处理文档后执行完整自查清单。

### 5.2 用户追问时的自查

当用户追问"有没有漏的""是不是全了""再检查一下"时，AI 必须：

1. **重新执行完整自查清单**，不是简单回答"已经检查过了"。
2. **扩大扫描范围**：首次只查了日报，追问时增加会议纪要和周报交叉校验。
3. **输出差异报告**：与首次处理结果的差异。

### 5.3 批量处理时的自查

当一次性处理多份文档时（如全团队日报）：

1. 每份文档单独执行自查。
2. 全部处理完后执行汇总自查（合并去重、交叉校验）。
3. 输出汇总自查清单。

---

## 6. 遗漏补救规则

### 6.1 发现遗漏时

1. **立即补充**，不等用户确认（低风险项）或立即建议补充（高风险项）。
2. 在自查清单中标注"⚠️ 首次遗漏，已补充"。
3. 在 AI 操作日志中记录遗漏原因和补救动作。

### 6.2 遗漏原因分析

AI 在发现遗漏后，应分析遗漏原因：

| 原因类型 | 说明 | 改进措施 |
|---|---|---|
| 关键词未覆盖 | 用户的表述不在信号词列表中 | 补充信号词 |
| 语义理解偏差 | AI 理解了字面意思但未识别为管理信号 | 标注为经验教训 |
| 处理中断 | 文档过长导致后半部分被忽略 | 分段处理，每段后自查 |
| 格式异常 | 文档格式不规范导致提取失败 | 标注为不确定项，请求用户确认 |

### 6.3 遗漏统计

AI 操作日志中应记录每次处理的遗漏率：

```markdown
## 自查统计
- 处理文档数：5
- 自查项总数：50
- 首次通过：47
- 首次遗漏：3（已全部补充）
- 遗漏率：6%
```

---

## 7. 索引过期检测

### 7.1 过期判断规则

| 索引文件 | 过期阈值 | 过期处理 |
|---|---|---|
| `todos/{date}/_index.md`（绑定文件） | 24 小时 | 提示「绑定文件上次更新 X 小时前，建议刷新」 |
| `YYYYMM/index.md`（v3.4.0 停维） | — | 不再实时同步；存量可留 |
| `pm-decisions.md` 八块开放行 | 7 天 | 超 7 天未裁定进入**日常提醒**（每日催办，合入 `19-info-completeness-rules.md` P0-P3）；超 14 天未裁定升级为 **critical**（P0/P1）。**八块都算**，不只块 8 |

### 7.2 过期提示格式

```markdown
## 索引状态检测

| 索引文件 | 上次更新 | 状态 | 建议 |
|---|---|---|---|
| todos/2026-08-08/_index.md | 2026-08-08 10:00 | ⚠️ 过期（25h） | 建议刷新 |
| todos/2026-08-09/_index.md | 2026-08-09 09:00 | ✅ 正常 | - |

是否现在刷新过期索引？
```

---

## 8. 与其他规则的关系

| 规则 | 自查时的职责 |
|---|---|
| `01-daily-report-rules.md` | 日报自查清单 D1–D31 |
| `02-meeting-rules.md` | 会议自查清单 M1-M7 |
| `05-query-rules.md` | 索引优先、禁止边查边建 |
| `10-update-trigger-rules.md` | 触发完整性（信号词全覆盖） |
| `11-output-artifact-rules.md` | 输出物生成后自查 |
| `13-continuity-rules.md` | 历史导入完整性校验 |
