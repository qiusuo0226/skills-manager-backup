# 打包 / 基线 / 审计共用。改这里，不要改脚本里的名单。
[exclude]
dirs = .git, governance, tests, outputs, __pycache__, .idea, .vscode, .qoder
files = .gitignore, .DS_Store, Thumbs.db, AGENTS.md
exts = .pyc, .pyo, .zip, .tar, .gz
