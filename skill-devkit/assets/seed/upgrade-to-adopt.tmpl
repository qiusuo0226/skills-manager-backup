# upgrade-to-__VERSION__

| 字段 | 值 |
|---|---|
| 目标版本 | __VERSION__ |
| 上一版本 | __VERSION__（同一行为版本，仅补开发规范） |
| CR | CR-000-adopt |
| 日期 | __DATE__ |

## 这版改了什么

技能 `__NAME__`（__DISPLAY_NAME__）收编进开发规范：版本控制、冻结基线、升级记录、变更门禁、打包脚本。**技能怎么干活没有改。**

## 使用者要做什么

- [x] 无需重写规则
- [x] 无需迁移工作区
- [ ] 若要用新的开发流程（先方案后改文件），在本开发仓操作即可

## 不兼容说明

无行为不兼容。开发方式变为：先出升级方案，人同意执行或另开对话审核后再改技能。

## 回滚

删除本轮收编提交（或对照收编前 git）。不要在半套 `governance/` 上打补丁。
