# Change Request

## Basic Info

| Field | Value |
|---|---|
| CR ID | CR-20260907-001 |
| Current Version | 0.8.0 |
| Target Version | 0.9.0 |
| Requester | 仇索 |
| Created At | 2026-09-07 |
| Change Type | feature |
| Priority | P1 |
| Status | completed |
| 执行授权 | 人直接同意（未经 B）。用户说「可以了执行升级吧，直接全升级完」。AP `upgrade-plan-v0.9.0.md` 1.1 |

## Change Goal

skill-devkit 0.9.0：发版前自动结构校验。目标仓 audit 先跑 `validate_skill.py`；本仓用同一份脚本对着仓根跑（仍不跑完整 `audit_release.py`）。

## Problem Statement

发版检查只拦版本不一致、缺基线、治理目录进包。`SKILL.md` 缺 name/description、正文点到的 `references/` 文件不存在，要装上或用起来才发现。本包根又不能跑 seed `audit_release.py`（无基线），缺少一条不依赖基线的结构检查。

## Scope

见 `CHANGELOG.md` 0.9.0 段、`governance/migrations/upgrade-to-0.9.0.md`、本 CR 与 IA/RR。方案 `governance/planning/upgrade-plan-v0.9.0.md` AP 1.1（发版后删除）。

## Non-goals

- 不给已用 0.8.x 初始化的目标仓自动打补丁
- 不加 GitHub Actions / `.github/`
- 不写 README「自举验证」专节
- 不把对话回归表改成 pytest
- 本包根不跑 seed `audit_release.py`（无 `governance/baselines/`）
- 不改 `SKILL.md` 唤起 / 路由 / 硬闸
- 不做内容质量或文风检查

## Acceptance Criteria

1. `python assets/seed/validate_skill.py --skill-root .` 退出 0
2. 临时目录：缺 frontmatter name、VERSION 与 skill.json 不同步、断 `references/` 引用 → 退出非 0
3. `references/01-init.md` §5 含 `validate_skill.py` → `governance/scripts/` 与 `test_validate_skill.py` → `tests/`
4. `python tests/run_smoke.py` 退出 0
5. `python assets/seed/pack.py --skill-root . --dry-run` 不含 governance/.git/AGENTS.md

## Approval

- [x] approved（人直接同意，未经 B）
