# Change Request

## Basic Info

| Field | Value |
|---|---|
| CR ID | CR-20260904-001 |
| Current Version | 0.7.0 |
| Target Version | 0.8.0 |
| Requester | 仇索 |
| Created At | 2026-09-04 |
| Change Type | contract_change / feature |
| Priority | P1 |
| Status | completed |
| 执行授权 | 人直接同意（B3 通过-待修订；A 已出 AP 1.3 吸收 B1+B2+B3；用户说「可以了执行升级吧」。B4 结语「通过」；两条执行注意已在收尾补入：audit 三处加载相等断言、记录写明 B4） |

## Change Goal

skill-devkit 0.8.0：技能目录探测可配置；初始化/收编半套可续跑；打包排除以 `governance/pack.ini` 为单源；版权默认读 git user.name；description 触发表收缩；pack/探测可机器冒烟。

## Problem Statement

占用检查与收编安装目录闸绑死 `.grok`；半套落盘被硬闸 1 卡死；三脚本 `EXCLUDE_*` 手抄；版权默认写死「仇索」；front matter 触发表过长；tests/ 不可执行。

## Scope

见 `CHANGELOG.md` 0.8.0 段、`governance/migrations/upgrade-to-0.8.0.md`、本 CR 与 IA/RR。方案 `governance/planning/upgrade-plan-v0.8.0.md` AP 1.3（发版后删除；B1+B2+B3 通过-待修订已吸收；B4 通过，两条注意项收尾已补）。

## Non-goals

- 不给已用 0.7.x 初始化的目标仓自动打补丁
- 不硬编码灵犀绝对路径
- 确认前不把问答进度落盘
- 不把对话回归表改成 pytest
- 本包根不跑 seed `audit_release.py`（无 `governance/baselines/`）

## Acceptance Criteria

1. 占用检查 / 收编安装闸 / 试用拷贝只读 `references/03-skill-roots.md` 与 `scripts/discover_skill_roots.py`
2. 半套（种子痕迹 + 无非空版本基线）可续跑；已规范（痕迹 + 非空基线，含 VERSION 领先或删 rules/）仍停
3. 三脚本经 `pack_exclude.py` 读 `governance/pack.ini`；空 dirs 回退内置且 audit FAIL
4. 版权默认来自 `git config user.name`，空则不问仇索
5. `tests/run_smoke.py` 退出 0；`python assets/seed/pack.py --skill-root . --dry-run` 不含 governance/.git/AGENTS.md

## Approval

- [x] approved（人同意执行；B1+B2+B3 通过-待修订，AP 1.3 已吸收；B4 通过）
