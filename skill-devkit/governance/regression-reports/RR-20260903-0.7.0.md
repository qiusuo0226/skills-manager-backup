# RR 0.7.0

对照 `tests/adopt.md`、`tests/upgrade-roles.md`、`tests/gap-capture.md`。

已核：三脚本 `EXCLUDE_DIRS` 含 `outputs` 且三者字符串一致（X1）。

X2 须在目标仓有 `outputs/skill-gaps/` 时跑 seed 脚本副本；本包根没有那套脚本，不在本包根冒跑。

发版核验（B4）：AP-4 逐项已落盘；X1 三脚本 `EXCLUDE_DIRS` 一致且含 `outputs`。中间 tag `v0.7.0-step1` 保留，最终发布点为 `v0.7.0`。
