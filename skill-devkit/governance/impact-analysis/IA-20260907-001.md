# Impact Analysis IA-20260907-001

对应 CR-20260907-001 / AP `upgrade-plan-v0.9.0.md` 1.1。

## Summary

本包种子增加结构校验脚本，目标仓 audit 先跑它。本包 `SKILL.md` 不改。`skill.json` 仅同步版本号与 versionHistory。

## Affected Layers

| Layer | Affected | Notes |
|---|---|---|
| Contract (`SKILL.md` / `skill.json`) | yes | 仅 `skill.json` 版本；`SKILL.md` 不动 |
| Rules (`references/`) | yes | 01-init 拷贝表；02-adopt 测试补缺 |
| Templates (`assets/`) | yes | 种子 validate / 测试 / audit / dev.ps1 |
| Scripts (`scripts/`) | no | |
| Tests (`tests/`) | yes | `test_validate_skill.py` |
| Pack / version scripts | yes | 目标仓 audit 串联 validate；pack 排除不变 |

## Affected Capabilities

| Capability | Impact | Risk | Need Regression |
|---|---|---|---|
| 发布审计 | medium | 误报拦发版；漏拷脚本 audit FAIL | yes |
| 初始化拷贝 | medium | 表漏行则新仓无校验 | yes |
| 收编补缺 | low | 已有 tests 不覆盖；缺则补 | yes |
| 打包排除 | none | 脚本在 governance/ 下，本就不进 zip | yes（旧 dry-run） |
| 自动唤起 | none | SKILL.md 未改 | no |

## Compatibility

- [x] 不影响已安装的旧版使用者（须重装本包才用上新种子）
- [x] 需要使用者重装 Skill（要用 0.9.0 能力）
- [x] 无需工作区迁移

已用 0.8.x 初始化的目标仓不会自动长出 `validate_skill.py`。

## Rollback Plan

1. `git checkout v0.8.0`
2. 本包对照物是 tag，不是 `governance/baselines/`
