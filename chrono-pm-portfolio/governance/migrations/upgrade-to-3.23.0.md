# 升级到 3.23.0（Portfolio 锁步）

> 与 ChronoPM-Project 3.23.0 共用版本线。施工主清单见开发仓 `ChronoPM-Project/governance/migrations/upgrade-to-3.23.0.md`。  
> 本包行为：02 号 as-of + L16/L21 `auto` 按已确认。禁止写 `projects/*/ai`。禁止 `portfolio/cache/`。schema **0.16.0**。

### 收尾补记（2026-09-01，不另起版本）

- 用户核验通过。Grok 安装区不代更。禁写 `projects/*/ai`。禁止 `portfolio/cache/`。
