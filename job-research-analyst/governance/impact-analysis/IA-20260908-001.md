# Impact Analysis

## CR ID

CR-20260908-001

## Summary

契约文件 frontmatter 格式修复：`description` 从 YAML 折叠标量改为单行双引号，命中词文本不变。需重装以刷新触发索引。无工作区迁移。

## Affected Layers

| Layer | Affected | Notes |
|---|---|---|
| Contract (`SKILL.md` / `skill.json`) | yes | 仅 YAML 标量形式与 version；description 文本、硬闸、工作流不改 |
| Rules (`references/`) | no | |
| Templates (`assets/`) | no | |
| Scripts (`scripts/`) | no | |
| Tests (`tests/`) | yes | 新增 frontmatter 测试；既有 description 长度测试随格式更新 |
| Pack / version scripts | no | 不改 pack 排除规则 |

## Affected Capabilities

| Capability | Impact | Risk | Need Regression |
|---|---|---|---|
| 自动触发 | high | 索引仍空 / 误伤命中词 | yes |
| 三件套交付 | none | 未改模板与校验 | yes（确认未破坏） |
| 即时问答 / 口头确认 | none | 判定树未改 | yes（规则对照） |
| 缺口捕捉 | none | — | no |

## Compatibility

- [ ] 不影响已安装的旧版使用者
- [x] 需要使用者重装 Skill
- [ ] 需要工作区迁移（默认无）

## Rollback Plan

1. 回到基线 `governance/baselines/0.2.0/`
2. 重装 0.2.0 包或检出 `v0.2.0`
3. 用户已生成的 docx/xlsx 不改
