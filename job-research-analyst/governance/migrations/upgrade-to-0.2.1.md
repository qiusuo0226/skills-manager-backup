# upgrade-to-0.2.1

| 字段 | 值 |
|---|---|
| 目标版本 | 0.2.1 |
| 上一版本 | 0.2.0 |
| CR | CR-20260908-001 |
| 日期 | 2026-09-08 |

## 这版改了什么

`SKILL.md` 的 YAML `description` 从折叠标量 `>` 改为单行双引号。命中词文本（679 字）与 0.2.0 相同。判定树、硬闸、三件套、校验脚本未改。用于修复 Qoder 等宿主把 `>` 当成空描述、从而永不自动触发的问题。

## 使用者要做什么

- [ ] 用新 zip / 新目录**覆盖安装**已装技能（必须重装，否则索引可能仍是 `>`）
- [ ] 无需迁移工作区
- [ ] 重装后执行 `skill list`，描述应为完整可读文本，不得仅为 `>`
- [ ] 发布者：若广场「描述」就是激活字段，须贴与 `SKILL.md` YAML `description` **同文**（不要用 README 稍长稿）

## 不兼容说明

无。相对 0.2.0 的设计行为是修复。不重装则行为与 0.2.0 安装副本相同。

## 回滚

回到 0.2.0 分发包，对照 `governance/baselines/0.2.0/`。用户已生成的 docx/xlsx 不改。
